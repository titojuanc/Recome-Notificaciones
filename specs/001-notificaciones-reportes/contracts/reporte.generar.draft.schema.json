{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://recome.internal/schemas/reporte.generar.draft.json",
  "title": "reporte.generar (DRAFT — pendiente de ratificación en api-general)",
  "type": "object",
  "required": ["reporte_id", "tipo", "formato", "usuario_solicitante", "filtros"],
  "properties": {
    "reporte_id": {
      "type": "string",
      "format": "uuid",
      "description": "Identificador único del job de reporte, usado también para deduplicación."
    },
    "tipo": {
      "type": "string",
      "enum": ["ventas", "actividad_usuario", "catalogo_uso", "recomendaciones"],
      "description": "Debe existir en el registro de generadores (FR-010). Otros valores se rechazan con estado failed / motivo tipo_no_soportado."
    },
    "formato": {
      "type": "string",
      "enum": ["pdf", "excel"]
    },
    "usuario_solicitante": {
      "type": "string",
      "description": "Identificador del usuario dueño del reporte, usado para el prefijo tipo/fecha/usuario en MinIO."
    },
    "filtros": {
      "type": "object",
      "description": "Filtros/IDs a usar al consultar datos vía REST a api-general. NUNCA contiene los datos completos (FR-011).",
      "additionalProperties": true
    }
  },
  "additionalProperties": false
}
