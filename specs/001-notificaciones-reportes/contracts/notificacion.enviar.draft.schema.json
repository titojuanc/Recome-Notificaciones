{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://recome.internal/schemas/notificacion.enviar.draft.json",
  "title": "notificacion.enviar (DRAFT — pendiente de ratificación en api-general)",
  "type": "object",
  "required": ["event_id", "tipo_evento", "usuario_destino", "variables"],
  "properties": {
    "event_id": {
      "type": "string",
      "format": "uuid",
      "description": "Identificador único del evento, usado para deduplicación (FR-005)."
    },
    "tipo_evento": {
      "type": "string",
      "description": "Clave que selecciona el template de notificación (FR-002). Debe existir en el registro de templates."
    },
    "usuario_destino": {
      "type": "string",
      "description": "Identificador del usuario en api-general."
    },
    "idioma": {
      "type": "string",
      "enum": ["es", "en"],
      "description": "Opcional. Si está presente, prioriza sobre la preferencia consultada a api-general (FR-009)."
    },
    "variables": {
      "type": "object",
      "description": "Variables de reemplazo del template. Nunca texto libre (FR-002).",
      "additionalProperties": true
    },
    "canales_solicitados": {
      "type": "array",
      "items": { "type": "string", "enum": ["push", "mail"] },
      "description": "Opcional. Si se omite, se envía por todos los canales configurados para el tipo_evento."
    }
  },
  "additionalProperties": false
}
