{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://recome.internal/schemas/reporte.listo.draft.json",
  "title": "reporte.listo (DRAFT — pendiente de ratificación en api-general)",
  "type": "object",
  "required": ["reporte_id", "estado"],
  "properties": {
    "reporte_id": {
      "type": "string",
      "format": "uuid"
    },
    "estado": {
      "type": "string",
      "enum": ["ready", "error"]
    },
    "motivo": {
      "type": "string",
      "enum": ["timeout", "dependencia_no_disponible", "tipo_no_soportado", "limite_tamano_excedido"],
      "description": "Obligatorio si estado == error. timeout y limite_tamano_excedido son definitivos (sin reintento automático, FR-012/FR-013a). dependencia_no_disponible se reintenta automáticamente con backoff antes de llegar a este estado (FR-012a). tipo_no_soportado no se reintenta."
    },
    "usuario_solicitante": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "allOf": [
    {
      "if": { "properties": { "estado": { "const": "error" } } },
      "then": { "required": ["motivo"] }
    }
  ]
}
